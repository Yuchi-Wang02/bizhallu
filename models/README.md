# Models

This directory stores model metadata and run notes, not model weights.

Qwen3-0.6B weights are stored in the external Hugging Face cache:

```text
C:\Users\yuchi\Downloads\p1\hf_cache\hub\models--Qwen--Qwen3-0.6B
```

Keeping weights outside the project prevents the repository from becoming too
large and keeps the experiment easier to package.
