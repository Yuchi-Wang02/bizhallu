# Site

This directory contains the static HTML reading layer for BizHallu.

Open `index.html` directly in a browser. The Markdown files under `docs/`
remain the source-of-truth technical documents; this site is the polished
portfolio view.
